# -*- mode: snippet -*-
# name: comment
# key: comment
# --
`(let ((i 0)(line "") (comment (if (string-match "[ \t\n\r]+\\'" comment-start)(replace-match "" t t comment-start)comment-start))) (while (< i (/ fill-column (length comment))) (setq line (concat line  comment)) (setq i (1+ i)))line)`