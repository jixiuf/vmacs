#!/bin/bash
# if ls /dev/hidraw* &> /dev/null; then
#     # use yubikey
#     sudo -u jixiuf sh -c 'cd ~/Documents/jianguo/jianguo/keepass/gpg-backup/;make restore-yubikey'
# else
#     sudo -u jixiuf sh -c 'cd ~/Documents/jianguo/jianguo/keepass/gpg-backup/;make restore'
# fi
