#!/bin/bash

ykinfo -s
if [ $? -eq 0 ]; then
    # use yubikey
    sudo -u jixiuf sh -c 'cd ~/Documents/jianguo/jianguo/keepass/gnupg/;git checkout master'
else
    sudo -u jixiuf sh -c 'cd ~/Documents/jianguo/jianguo/keepass/gnupg/;git checkout HEAD~1'
fi
